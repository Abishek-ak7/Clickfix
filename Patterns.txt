const maliciousPatterns = [
  {
    pattern: /powershell(\.exe)?\s*-[Ee]ncode[d]?[Cc]ommand/i,
    priority: 1,
    description: "Detected attempt to execute encoded PowerShell commands within the webpage, often used to obfuscate malicious scripts."
  },
  {
    pattern: /cmd\s*\/c\s*start\s*\/min\s*powershell/i,
    priority: 1,
    description: "Detected attempt to silently launch PowerShell from the webpage using CMD, a common malware tactic."
  },
  {
    pattern: /\.(ps1|vbs|js)\s*['\"][^'\"]*?\$|%[^'\"]*?['\"]/i,
    priority: 3,
    description: "Detected script file references with suspicious variable usage, indicating potential obfuscation within the webpage."
  },
  {
    pattern: /setClipboardCopyData/i,
    priority: 1,
    description: "Detected clipboard manipulation attempt via the webpage, possibly for data exfiltration or phishing."
  },
  {
    pattern: /navigator\.writetext/i,
    priority: 2,
    description: "Detected use of clipboard API to write text from the webpage, potentially used in clipboard hijacking."
  },
  {
    pattern: /clipboard.*copy/i,
    priority: 2,
    description: "Detected generic clipboard copy operation initiated by the webpage, which may be used to steal or manipulate user data."
  },
  {
    pattern: /blur\(.*\)\s*.*clipboard/i,
    priority: 2,
    description: "Detected clipboard access triggered by blur events in the webpage, which may be used to stealthily capture data."
  },
  {
    pattern: /base64decode/i,
    priority: 2,
    description: "Detected base64 decoding operation in the webpage, often used to hide malicious payloads."
  },
  {
    pattern: /(win(dows)?\s*(icon|key)?\s*\+\s*r)/i,
    priority: 2,
    description: "Detected reference to Windows key shortcuts in the webpage, possibly used in social engineering or keylogging."
  },
  {
    pattern: /keyCode\s*[:=]\s*(82|91)/i,
    priority: 2,
    description: "Detected JavaScript key codes for 'R' or 'Windows' keys in the webpage, often used in keylogging or shortcut hijacking."
  },
  {
    pattern: /event\.(ctrlKey|metaKey)/i,
    priority: 2,
    description: "Detected script in the webpage monitoring control/meta key presses, which may be used to trigger malicious actions."
  },
  {
    pattern: /new\s*ActiveXObject/i,
    priority: 1,
    description: "Detected creation of ActiveX objects from the webpage, commonly used in older malware for system access."
  },
  {
    pattern: /WScript\.Shell/i,
    priority: 1,
    description: "Detected use of Windows Script Host Shell from the webpage, often used to execute system-level commands."
  },
  {
    pattern: /regsvr32/i,
    priority: 1,
    description: "Detected use of regsvr32 command from the webpage, which can be abused to execute DLLs or scripts."
  },
  {
    pattern: /certutil\s.*-decode/i,
    priority: 1,
    description: "Detected use of certutil to decode files via the webpage, a known method for downloading and decoding malware."
  },
  {
    pattern: /document\.execCommand\s*\(\s*['\"]copy['\"]/i,
    priority: 2,
    description: "Detected use of execCommand to copy content to clipboard from the webpage, potentially used in clipboard hijacking."
  },
  {
    pattern: /navigator\.clipboard\.writeText/i,
    priority: 2,
    description: "Detected direct clipboard manipulation via modern API from the webpage, which may be used for data injection."
  },
  {
    pattern: /eval\s*\(\s*atob\s*\(/i,
    priority: 1,
    description: "Detected base64-decoded code execution using eval in the webpage, a common obfuscation technique."
  },
  {
    pattern: /\.replace\s*\(\s*['\"]javascript:/i,
    priority: 1,
    description: "Detected attempt to inject or manipulate JavaScript URLs in the webpage, often used in XSS or phishing."
  }
];